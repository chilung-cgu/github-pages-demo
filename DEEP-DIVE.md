# GitHub Pages & GitHub Actions 深度教學

這是一份從基礎概念到實際應用的完整教學，幫助你深入理解 GitHub Pages 和 GitHub Actions 的運作原理。

---

## 📚 第一部分：GitHub Pages 基礎概念

### 1.1 GitHub Pages 是什麼？

**GitHub Pages** 是 GitHub 提供的**免費靜態網站託管服務**。

#### 關鍵概念：「靜態網站」是什麼意思？

```
┌─────────────────────────────────────────────────────────────┐
│                    網站類型比較                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  靜態網站 (Static Site)         動態網站 (Dynamic Site)     │
│  ┌─────────────────┐            ┌─────────────────┐         │
│  │   HTML/CSS/JS   │            │  Server (PHP,   │         │
│  │   檔案直接送給   │            │  Python, Node)  │         │
│  │   瀏覽器顯示     │            │       ↓         │         │
│  └─────────────────┘            │  根據請求產生    │         │
│                                 │  不同的 HTML     │         │
│  ✅ 快速、安全                  └─────────────────┘         │
│  ✅ 無需資料庫                                              │
│  ✅ 免費託管                    需要伺服器運算               │
│  ❌ 無法即時互動                需要資料庫                   │
│                                 有運維成本                   │
└─────────────────────────────────────────────────────────────┘
```

**GitHub Pages 適合：**
- 個人部落格、作品集
- 專案文檔
- 簡單的展示網站
- 不需要後端資料庫的網站

**GitHub Pages 不適合：**
- 需要使用者登入的系統
- 電商網站（需要資料庫）
- 即時聊天應用

---

### 1.2 GitHub Pages 的程式碼從哪裡來？

這是你問的關鍵問題！讓我用圖解說明：

```
┌────────────────────────────────────────────────────────────────────┐
│                   GitHub Pages 的資料流向                           │
└────────────────────────────────────────────────────────────────────┘

  你的 Repository                          GitHub Pages 伺服器
  ┌────────────────┐                      ┌────────────────────┐
  │ github-pages-  │     ──建置部署──>     │ 託管你的靜態檔案    │
  │ demo/          │                      │                    │
  │  ├── index.html│                      │ https://chilung-   │
  │  ├── style.css │                      │ cgu.github.io/     │
  │  └── ...       │                      │ github-pages-demo/ │
  └────────────────┘                      └────────────────────┘
         │                                         │
         │ Git Push                                │ 使用者訪問
         ▼                                         ▼
  ┌────────────────┐                      ┌────────────────────┐
  │ 你的電腦/      │                      │ 訪客的瀏覽器        │
  │ 開發環境       │                      │ (任何人都可以看)    │
  └────────────────┘                      └────────────────────┘
```

**重點理解：**
1. **Repository 就是原始碼來源** - 你在 GitHub Repository 中的檔案（HTML, CSS, JS）就是網站的內容來源
2. **GitHub Pages 服務** 會把這些檔案複製到他們的伺服器上
3. **分配一個 URL** 給你：`https://[username].github.io/[repo-name]/`

---

## 📚 第二部分：兩種部署方式的差異

這是你問的另一個關鍵問題！GitHub Pages 有兩種 Source（來源）設定：

### 2.1 Deploy from a Branch（從分支部署）

```
┌─────────────────────────────────────────────────────────────────┐
│                    Deploy from a Branch                          │
└─────────────────────────────────────────────────────────────────┘

  Repository                                  GitHub Pages
  ┌────────────────┐                         
  │ main branch    │                         
  │  ├── /docs     │  ← 你可以指定這個資料夾  
  │  │   └── *.html│    作為網站根目錄        
  │  └── 其他檔案   │                         
  └───────┬────────┘                         
          │                                  
          ▼                                  
  ┌────────────────┐      自動      ┌─────────────────┐
  │ GitHub 內建的   │  ──────────>  │ 網站上線        │
  │ Jekyll 建置流程 │               │                 │
  │ (黑盒子)        │               │ ✅ 簡單設定即可  │
  └────────────────┘               │ ❌ 無法自訂流程  │
                                   └─────────────────┘
```

**設定方式：**
- Settings → Pages → Source → 選擇 Branch（如 `main`）
- 可選擇根目錄 `/` 或 `/docs` 資料夾

**特點：**
| 優點 | 缺點 |
|------|------|
| ✅ 設定超簡單 | ❌ 無法自訂建置流程 |
| ✅ 適合純 HTML 網站 | ❌ 內建只支援 Jekyll |
| ✅ 不需要寫任何設定檔 | ❌ 無法使用其他框架 (React, Vue 等) |

---

### 2.2 GitHub Actions（使用工作流程部署）

```
┌─────────────────────────────────────────────────────────────────┐
│                    GitHub Actions 部署                           │
└─────────────────────────────────────────────────────────────────┘

  Repository                                  
  ┌────────────────────────────────┐         
  │ main branch                    │         
  │  ├── .github/                  │  ← 工作流程設定檔
  │  │   └── workflows/            │    
  │  │       └── deploy.yml        │  ← 你的部署腳本
  │  ├── index.html                │         
  │  └── ...                       │         
  └───────────────┬────────────────┘         
                  │ git push                 
                  ▼                          
  ┌────────────────────────────────┐         
  │     GitHub Actions Runner      │ ← 執行你定義的步驟
  │  ┌────────────────────────┐    │         
  │  │ 1. Checkout 程式碼     │    │         
  │  │ 2. 安裝相依套件        │    │  你可以完全
  │  │ 3. 建置專案            │    │  自己定義！
  │  │ 4. 部署到 Pages        │    │         
  │  └────────────────────────┘    │         
  └───────────────┬────────────────┘         
                  ▼                          
  ┌────────────────────────────────┐         
  │         網站上線               │         
  │  ✅ 完全可自訂                 │         
  │  ✅ 支援任何框架               │         
  │  ✅ 可加入測試、檢查           │         
  └────────────────────────────────┘         
```

**設定方式：**
- 在 Repository 中創建 `.github/workflows/deploy.yml`
- Settings → Pages → Source → 選擇 **GitHub Actions**

**你的 `deploy.yml` 解析：**

```yaml
# ===== 觸發條件 =====
on:
  push:
    branches: ["main"]      # 當推送到 main 分支時觸發
  workflow_dispatch:        # 允許手動觸發

# ===== 執行步驟 =====
jobs:
  deploy:
    runs-on: ubuntu-latest  # 在 Ubuntu 虛擬機上執行
    
    steps:
      # 步驟 1: 下載你的程式碼
      - name: Checkout
        uses: actions/checkout@v4
      
      # 步驟 2: 設定 GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      # 步驟 3: 打包檔案準備上傳
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'  # 上傳整個 repository 的檔案
      
      # 步驟 4: 部署到 GitHub Pages
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
```

---

### 2.3 兩種方式的完整比較

| 特性 | Deploy from Branch | GitHub Actions |
|------|-------------------|----------------|
| **設定難度** | 🟢 超簡單（點幾下就好） | 🟡 需要寫 YAML 設定檔 |
| **彈性** | 🔴 低 | 🟢 完全自訂 |
| **支援框架** | 只有 Jekyll | React, Vue, Next.js, Hugo 等全部 |
| **建置過程可見性** | 🔴 黑盒子看不到 | 🟢 完全透明可 debug |
| **適合場景** | 簡單靜態網站 | 複雜專案、需要建置的專案 |
| **可加入測試** | ❌ 不行 | ✅ 可以 |
| **可加入驗證/審核** | ❌ 不行 | ✅ 可以 |

---

## 📚 第三部分：GitHub Actions 完整解析

### 3.1 GitHub Actions 是什麼？

**GitHub Actions** 是 GitHub 的 **CI/CD 自動化平台**：

- **CI** = Continuous Integration（持續整合）= 自動測試程式碼
- **CD** = Continuous Deployment（持續部署）= 自動部署程式碼

```
┌─────────────────────────────────────────────────────────────────┐
│                 GitHub Actions 運作流程                          │
└─────────────────────────────────────────────────────────────────┘

  開發者           GitHub                   Runner (虛擬機)
    │                │                          │
    │ git push       │                          │
    │───────────────>│                          │
    │                │                          │
    │                │  偵測到 .github/workflows │
    │                │  中有相符的觸發條件        │
    │                │                          │
    │                │  啟動 Runner             │
    │                │─────────────────────────>│
    │                │                          │
    │                │                          │ 執行 deploy.yml
    │                │                          │ 中定義的步驟
    │                │                          │
    │                │        完成通知          │
    │                │<─────────────────────────│
    │                │                          │
    │  ✅ 或 ❌ 結果 │                          │
    │<───────────────│                          │
```

### 3.2 Workflow 檔案結構

```yaml
# .github/workflows/deploy.yml 結構解析

name: 工作流程名稱    # 顯示在 Actions tab 的名稱

on:                   # 觸發條件
  push:               # 當 push 時觸發
    branches: [main]  # 只有 main 分支
  pull_request:       # 當 PR 時觸發 (可選)
  schedule:           # 排程觸發 (可選)
    - cron: '0 0 * * *'  # 每天午夜
  workflow_dispatch:  # 允許手動觸發

permissions:          # 權限設定
  contents: read      # 可以讀取 repo 內容
  pages: write        # 可以寫入 GitHub Pages

jobs:                 # 定義工作
  job-name:           # 工作名稱
    runs-on: ubuntu-latest  # 執行環境
    
    steps:            # 執行步驟
      - name: 步驟名稱
        uses: action/name@version  # 使用現成的 action
        
      - name: 執行指令
        run: |        # 執行 shell 指令
          npm install
          npm run build
```

### 3.3 常用的 Actions

| Action | 用途 |
|--------|------|
| `actions/checkout@v4` | 下載你的程式碼 |
| `actions/setup-node@v4` | 安裝 Node.js |
| `actions/configure-pages@v4` | 設定 GitHub Pages |
| `actions/upload-pages-artifact@v3` | 上傳要部署的檔案 |
| `actions/deploy-pages@v4` | 部署到 GitHub Pages |

---

## 📚 第四部分：實戰理解

### 4.1 當你執行 `git push` 時發生了什麼？

```
時間線
─────────────────────────────────────────────────────────────────>

  你執行          GitHub 收到       Actions          部署           網站
  git push        push 事件        Runner 啟動      完成           更新
     │               │                │              │              │
     ▼               ▼                ▼              ▼              ▼
   ┌───┐          ┌───┐           ┌─────┐        ┌───┐          ┌───┐
   │   │ ──────> │   │ ────────> │     │ ─────> │   │ ───────> │   │
   └───┘          └───┘           └─────┘        └───┘          └───┘
   
   ~1秒           ~2秒            10-60秒         ~5秒           即時

   Repository:                   Actions Tab:                   網站:
   有新的 commit                 顯示執行狀態                   內容更新
```

### 4.2 在 GitHub 網頁上觀察

你可以在這些地方觀察：

1. **Repository 首頁** - 看到最新的 commit
2. **Actions Tab** - 看到工作流程執行狀態
3. **Settings → Pages** - 看到部署後的網址

---

## 📚 第五部分：進階應用範例

### 5.1 React 專案的 GitHub Actions

如果你的專案是 React，workflow 會需要建置步驟：

```yaml
name: Deploy React App

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      # 安裝 Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      # 安裝相依套件
      - name: Install dependencies
        run: npm ci
      
      # 建置專案
      - name: Build
        run: npm run build
      
      # 上傳建置結果
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'  # React 建置輸出的資料夾
      
      - name: Deploy
        uses: actions/deploy-pages@v4
```

### 5.2 自動測試後再部署

```yaml
name: Test and Deploy

on:
  push:
    branches: ["main"]

jobs:
  # 先測試
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: npm test
  
  # 測試通過才部署
  deploy:
    needs: test  # 等 test job 完成
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v4
      - uses: actions/upload-pages-artifact@v3
      - uses: actions/deploy-pages@v4
```

---

## 📖 總結

### 核心概念整理

| 概念 | 說明 |
|------|------|
| **GitHub Pages** | GitHub 提供的免費靜態網站託管服務 |
| **靜態網站** | 只有 HTML/CSS/JS，無需伺服器運算 |
| **Source** | 網站內容的來源（Branch 或 Actions） |
| **GitHub Actions** | GitHub 的自動化執行平台 |
| **Workflow** | 定義在 `.github/workflows/*.yml` 的自動化流程 |
| **Runner** | 執行 workflow 的虛擬機器 |
| **Action** | 可重複使用的任務單元（如 `actions/checkout`） |

### 學習建議

1. ✅ **已完成** - 理解基本概念
2. ⏳ **下一步** - 嘗試修改網站內容，觀察自動部署
3. 🔮 **進階** - 學習 Jekyll、Hugo 等靜態網站生成器
4. 🚀 **高級** - 建立包含測試、部署審核的完整 CI/CD 流程
